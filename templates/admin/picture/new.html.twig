{% extends 'base.html.twig' %}

{% block title %}New Pictures{% endblock %}

{% block body %}
    <h1 class="text-center">Créer une nouvelle Photo</h1>

    {{ include('admin/picture/_form.html.twig') }}

    <a href="{{ path('admin_picture_index') }}">Retour</a>
{% endblock %}

{% block javascripts %}
    <script>
        $('.custom-file-input').on('change', function(event) {
            var inputFile = event.currentTarget;
            $(inputFile).parent()
                .find('.custom-file-label')
                .html(inputFile.files[0].name);
        });
    </script>
{% endblock %}