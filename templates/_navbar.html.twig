<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="{{ path('app_homeIndex') }}">Stéphane</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto"></ul>
        <div class="form-inline my-2 my-lg-0 mobile-nav">
            <a class="nav-link" href="https://www.linkedin.com/in/st%C3%A9phane-acloque-b679751a4/" target="_blank">Linkedin</a>
            <a class="nav-link" href="{{ path('message_new') }}">Contact</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-link" href="{{ path("project_index") }}">Admin</a>
            {% endif %}
        </div>
    </div>
</nav>
